EmailService.send_welcome_email(user)

EmailService.property_approved(property)

EmailService.property_rejected(property)

EmailService.booking_received(booking)

EmailService.subscription_activated(subscription)

EmailService.payment_receipt(payment)

EmailService.lease_expiring(lease)